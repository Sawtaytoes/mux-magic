{/* MoonIcon.mdx */}

import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as MoonIconStories from "./MoonIcon.stories";

<Meta of={MoonIconStories} />

# MoonIcon

A 16×16px crescent-moon SVG icon. The **dark**-mode glyph for the header colour-scheme switcher (`<ColorSchemeSwitcher>`). Inherits `currentColor` stroke so it respects the parent text color.

<Canvas of={MoonIconStories.Default} />
