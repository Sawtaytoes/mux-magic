{/* SunIcon.mdx */}

import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as SunIconStories from "./SunIcon.stories";

<Meta of={SunIconStories} />

# SunIcon

A 16×16px sun SVG icon. The **light**-mode glyph for the header colour-scheme switcher (`<ColorSchemeSwitcher>`). Inherits `currentColor` stroke so it respects the parent text color.

<Canvas of={SunIconStories.Default} />
