paths:
  basePath:
    label: Base Path
    value: /fixture/media
steps:
  - id: step-fixture
    command: splitChapters
    params: {sourcePath: '@basePath', chapterSplits: [ch1, ch2], isRenumberingChapters: false, isPaddingChapterNumbers: false}
