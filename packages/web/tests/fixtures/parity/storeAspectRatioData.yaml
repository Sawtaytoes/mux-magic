paths:
  basePath:
    label: Base Path
    value: /fixture/media
steps:
  - id: step-fixture
    command: storeAspectRatioData
    params: {sourcePath: '@basePath', recursiveDepth: 2, outputPath: '@basePath', rootPath: fixture-string-rootPath, folders: [folder-a, folder-b], force: true}
