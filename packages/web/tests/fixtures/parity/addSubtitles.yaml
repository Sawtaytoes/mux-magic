paths:
  basePath:
    label: Base Path
    value: /fixture/media
steps:
  - id: step-fixture
    command: addSubtitles
    params: {sourcePath: '@basePath', subtitlesPath: '@basePath', hasChapterSyncOffset: true, globalOffset: 100, includeChapters: true, offsets: [0, -200, 150]}
