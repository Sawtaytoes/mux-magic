paths:
  basePath:
    label: Base Path
    value: /fixture/music
steps:
  - id: step-fixture
    command: convertLosslessToFlac
    params: {sourcePath: '@basePath', isRecursive: true, isSourceDeleted: true}
